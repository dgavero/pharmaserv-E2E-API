# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: api/tests/rider/profile/rider.getRiderDocuments.spec.js >> GraphQL: Get Rider Documents >> PHARMA-319 | Should NOT be able to get rider documents with missing auth tokens
- Location: api/tests/rider/profile/rider.getRiderDocuments.spec.js:30:7

# Error details

```
Error: expect(received).toContain(expected) // indexOf

Expected value: 503
Received array: [401, 404, 403, 500]
```

# Test source

```ts
  1   | import { loginAsRiderAndGetTokens, NOAUTH_MESSAGE_PATTERN, NOAUTH_CLASSIFICATIONS, NOAUTH_CODES, NOAUTH_HTTP_STATUSES } from '../../../helpers/auth.js';
  2   | import { safeGraphQL, bearer, getGQLError } from '../../../helpers/graphqlUtils.js';
  3   | import { test, expect } from '../../../globalConfig.api.js';
  4   | import { getRiderCredentials } from '../../../helpers/roleCredentials.js';
  5   | import { RIDER_GET_DOCUMENTS_QUERY } from './rider.profileQueries.js';
  6   | 
  7   | test.describe('GraphQL: Get Rider Documents', () => {
  8   |   test(
  9   |     'PHARMA-318 | Should be able to get rider documents',
  10  |     {
  11  |       tag: ['@api', '@rider', '@positive', '@pharma-318'],
  12  |     },
  13  |     async ({ api }) => {
  14  |       const { accessToken, raw: loginRes } = await loginAsRiderAndGetTokens(api, getRiderCredentials('default'));
  15  |       expect(loginRes.ok, loginRes.error || 'Rider login failed').toBe(true);
  16  | 
  17  |       const getRiderDocumentsRes = await safeGraphQL(api, {
  18  |         query: RIDER_GET_DOCUMENTS_QUERY,
  19  |         headers: bearer(accessToken),
  20  |       });
  21  |       expect(getRiderDocumentsRes.ok, getRiderDocumentsRes.error || 'Get Rider Documents request failed').toBe(
  22  |         true
  23  |       );
  24  | 
  25  |       const documentsNode = getRiderDocumentsRes.body?.data?.rider?.documents;
  26  |       expect(Array.isArray(documentsNode), 'rider.documents should be an array').toBe(true);
  27  |     }
  28  |   );
  29  | 
  30  |   test(
  31  |     'PHARMA-319 | Should NOT be able to get rider documents with missing auth tokens',
  32  |     {
  33  |       tag: ['@api', '@rider', '@negative', '@pharma-319'],
  34  |     },
  35  |     async ({ api, noAuth }) => {
  36  |       const getRiderDocumentsNoAuthRes = await safeGraphQL(api, {
  37  |         query: RIDER_GET_DOCUMENTS_QUERY,
  38  |         headers: noAuth,
  39  |       });
  40  |       expect(getRiderDocumentsNoAuthRes.ok).toBe(false);
  41  | 
  42  |       if (!getRiderDocumentsNoAuthRes.httpOk) {
> 43  |         expect(NOAUTH_HTTP_STATUSES).toContain(getRiderDocumentsNoAuthRes.httpStatus);
      |                                      ^ Error: expect(received).toContain(expected) // indexOf
  44  |       } else {
  45  |         const { message, code, classification } = getGQLError(getRiderDocumentsNoAuthRes);
  46  |         expect(message).toMatch(NOAUTH_MESSAGE_PATTERN);
  47  |         expect.soft(NOAUTH_CODES).toContain(code);
  48  |         expect.soft(NOAUTH_CLASSIFICATIONS).toContain(classification);
  49  |       }
  50  |     }
  51  |   );
  52  | 
  53  |   test(
  54  |     'PHARMA-320 | Should NOT be able to get rider documents with invalid auth tokens',
  55  |     {
  56  |       tag: ['@api', '@rider', '@negative', '@pharma-320'],
  57  |     },
  58  |     async ({ api, invalidAuth }) => {
  59  |       const getRiderDocumentsInvalidAuthRes = await safeGraphQL(api, {
  60  |         query: RIDER_GET_DOCUMENTS_QUERY,
  61  |         headers: invalidAuth,
  62  |       });
  63  |       expect(getRiderDocumentsInvalidAuthRes.ok).toBe(false);
  64  | 
  65  |       if (!getRiderDocumentsInvalidAuthRes.httpOk) {
  66  |         expect(NOAUTH_HTTP_STATUSES).toContain(getRiderDocumentsInvalidAuthRes.httpStatus);
  67  |       } else {
  68  |         const { message, code, classification } = getGQLError(getRiderDocumentsInvalidAuthRes);
  69  |         expect(message).toMatch(NOAUTH_MESSAGE_PATTERN);
  70  |         expect.soft(NOAUTH_CODES).toContain(code);
  71  |         expect.soft(NOAUTH_CLASSIFICATIONS).toContain(classification);
  72  |       }
  73  |     }
  74  |   );
  75  | 
  76  |   test(
  77  |     'PHARMA-568 | Rider documents should satisfy response contract shape',
  78  |     {
  79  |       tag: ['@api', '@rider', '@positive', '@pharma-568'],
  80  |     },
  81  |     async ({ api }) => {
  82  |       const { accessToken, raw: loginRes } = await loginAsRiderAndGetTokens(api, getRiderCredentials('default'));
  83  |       expect(loginRes.ok, loginRes.error || 'Rider login failed').toBe(true);
  84  | 
  85  |       const getRiderDocumentsRes = await safeGraphQL(api, {
  86  |         query: RIDER_GET_DOCUMENTS_QUERY,
  87  |         headers: bearer(accessToken),
  88  |       });
  89  |       expect(getRiderDocumentsRes.httpStatus).toBe(200);
  90  |       expect(getRiderDocumentsRes.httpOk).toBe(true);
  91  |       expect(getRiderDocumentsRes.ok, getRiderDocumentsRes.error || 'Get Rider Documents request failed').toBe(true);
  92  | 
  93  |       const node = getRiderDocumentsRes.body?.data?.rider?.documents;
  94  |       expect(Array.isArray(node), 'Expected data.rider.documents to be an array').toBe(true);
  95  |       if (node.length > 0) {
  96  |         expect.soft(typeof node[0]?.type).toBe('string');
  97  |         expect.soft(typeof node[0]?.photo).toBe('string');
  98  |       }
  99  |     }
  100 |   );
  101 | });
  102 | 
```