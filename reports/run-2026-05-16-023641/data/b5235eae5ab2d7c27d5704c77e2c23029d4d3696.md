# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: api/tests/admin/assetsManagement/admin.updateAsset.spec.js >> GraphQL: Admin Update Asset >> PHARMA-252 | Should update asset with valid auth tokens
- Location: api/tests/admin/assetsManagement/admin.updateAsset.spec.js:16:7

# Error details

```
Error: 500: Microservice Server Error: 500 INTERNAL_SERVER_ERROR - Rider Service is temporarily unavailable. Please try again in a few minutes.

expect(received).toBe(expected) // Object.is equality

Expected: true
Received: false
```

# Test source

```ts
  1   | import { loginAsAdminAndGetTokens, NOAUTH_MESSAGE_PATTERN, NOAUTH_CLASSIFICATIONS, NOAUTH_CODES, NOAUTH_HTTP_STATUSES } from '../../../helpers/auth.js';
  2   | import { safeGraphQL, bearer, getGQLError } from '../../../helpers/graphqlUtils.js';
  3   | import { test, expect } from '../../../globalConfig.api.js';
  4   | import { getAdminCredentials } from '../../../helpers/roleCredentials.js';
  5   | import { UPDATE_ASSET_MUTATION } from './admin.assetsManagementQueries.js';
  6   | 
  7   | const UPDATE_ASSET_INPUT = {
  8   |   assetId: 1,
  9   |   asset: {
  10  |     status: 'INACTIVE',
  11  |     assignedTo: 'Doe, John',
  12  |   },
  13  | };
  14  | 
  15  | test.describe('GraphQL: Admin Update Asset', () => {
  16  |   test(
  17  |     'PHARMA-252 | Should update asset with valid auth tokens',
  18  |     {
  19  |       tag: ['@api', '@admin', '@positive', '@pharma-252'],
  20  |     },
  21  |     async ({ api }) => {
  22  |       const { accessToken, raw: loginRes } = await loginAsAdminAndGetTokens(api, getAdminCredentials('default'));
  23  |       expect(loginRes.ok, loginRes.error || 'Admin login failed').toBe(true);
  24  | 
  25  |       const updateAssetRes = await safeGraphQL(api, {
  26  |         query: UPDATE_ASSET_MUTATION,
  27  |         variables: UPDATE_ASSET_INPUT,
  28  |         headers: bearer(accessToken),
  29  |       });
  30  | 
> 31  |       expect(updateAssetRes.ok, updateAssetRes.error || 'Update asset endpoint failed').toBe(true);
      |                                                                                         ^ Error: 500: Microservice Server Error: 500 INTERNAL_SERVER_ERROR - Rider Service is temporarily unavailable. Please try again in a few minutes.
  32  |     }
  33  |   );
  34  | 
  35  |   test(
  36  |     'PHARMA-253 | Should NOT update asset with missing auth tokens',
  37  |     {
  38  |       tag: ['@api', '@admin', '@negative', '@pharma-253'],
  39  |     },
  40  |     async ({ api, noAuth }) => {
  41  |       const updateAssetNoAuthRes = await safeGraphQL(api, {
  42  |         query: UPDATE_ASSET_MUTATION,
  43  |         variables: UPDATE_ASSET_INPUT,
  44  |         headers: noAuth,
  45  |       });
  46  | 
  47  |       expect(
  48  |         updateAssetNoAuthRes.ok,
  49  |         updateAssetNoAuthRes.error || 'Expected UNAUTHORIZED when missing token'
  50  |       ).toBe(false);
  51  | 
  52  |       if (!updateAssetNoAuthRes.httpOk) {
  53  |         expect(NOAUTH_HTTP_STATUSES).toContain(updateAssetNoAuthRes.httpStatus);
  54  |       } else {
  55  |         const { message, code, classification } = getGQLError(updateAssetNoAuthRes);
  56  |         expect(message).toMatch(NOAUTH_MESSAGE_PATTERN);
  57  |         expect.soft(NOAUTH_CODES).toContain(code);
  58  |         expect.soft(NOAUTH_CLASSIFICATIONS).toContain(classification);
  59  |       }
  60  |     }
  61  |   );
  62  | 
  63  |   test(
  64  |     'PHARMA-254 | Should NOT update asset with invalid auth tokens',
  65  |     {
  66  |       tag: ['@api', '@admin', '@negative', '@pharma-254'],
  67  |     },
  68  |     async ({ api, invalidAuth }) => {
  69  |       const updateAssetInvalidAuthRes = await safeGraphQL(api, {
  70  |         query: UPDATE_ASSET_MUTATION,
  71  |         variables: UPDATE_ASSET_INPUT,
  72  |         headers: invalidAuth,
  73  |       });
  74  | 
  75  |       expect(updateAssetInvalidAuthRes.ok).toBe(false);
  76  |       expect(updateAssetInvalidAuthRes.httpOk).toBe(false);
  77  |       expect(NOAUTH_HTTP_STATUSES).toContain(updateAssetInvalidAuthRes.httpStatus);
  78  |     }
  79  |   );
  80  | 
  81  |   test(
  82  |     'PHARMA-438 | Should fail update asset when status enum is invalid',
  83  |     {
  84  |       tag: ['@api', '@admin', '@negative', '@pharma-438'],
  85  |     },
  86  |     async ({ api }) => {
  87  |       const { accessToken, raw: loginRes } = await loginAsAdminAndGetTokens(api, getAdminCredentials('default'));
  88  |       expect(loginRes.ok, loginRes.error || 'Admin login failed').toBe(true);
  89  | 
  90  |       const invalidUpdateAssetInput = {
  91  |         assetId: UPDATE_ASSET_INPUT.assetId,
  92  |         asset: {
  93  |           ...UPDATE_ASSET_INPUT.asset,
  94  |           status: 'INVALID_STATUS',
  95  |         },
  96  |       };
  97  | 
  98  |       const updateAssetInvalidInputRes = await safeGraphQL(api, {
  99  |         query: UPDATE_ASSET_MUTATION,
  100 |         variables: invalidUpdateAssetInput,
  101 |         headers: bearer(accessToken),
  102 |       });
  103 | 
  104 |       expect(updateAssetInvalidInputRes.ok).toBe(false);
  105 |       if (updateAssetInvalidInputRes.httpOk) {
  106 |         const { message, code, classification } = getGQLError(updateAssetInvalidInputRes);
  107 |         expect(message, 'Expected GraphQL validation message for invalid status').toBeTruthy();
  108 |         expect.soft(code || classification, 'Expected GraphQL validation code/classification').toBeTruthy();
  109 |       } else {
  110 |         expect.soft(updateAssetInvalidInputRes.httpStatus).toBeGreaterThanOrEqual(400);
  111 |       }
  112 |     }
  113 |   );
  114 | });
  115 | 
```