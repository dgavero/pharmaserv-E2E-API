# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: api/tests/pharmacy/profile/pharmacist.getMyPharmacy.spec.js >> GraphQL: Get My Pharmacy >> PHARMA-151 | Should NOT be able to get myPharmacy as Pharmacist with No Auth
- Location: api/tests/pharmacy/profile/pharmacist.getMyPharmacy.spec.js:30:7

# Error details

```
Error: expect(received).toMatch(expected)

Expected pattern: /(Unauthorized|Unauthorized Access|Access Denied|INTERNAL_SERVER_ERROR|invalid value|Access denied|not found|Bad Request)/
Received string:  ""
```

# Test source

```ts
  1  | import { loginAsPharmacistAndGetTokens, NOAUTH_MESSAGE_PATTERN, NOAUTH_CLASSIFICATIONS, NOAUTH_CODES, NOAUTH_HTTP_STATUSES } from '../../../helpers/auth.js';
  2  | import { safeGraphQL, bearer, getGQLError } from '../../../helpers/graphqlUtils.js';
  3  | import { test, expect } from '../../../globalConfig.api.js';
  4  | import { getPharmacistCredentials } from '../../../helpers/roleCredentials.js';
  5  | import { PHARMACIST_GET_MY_PHARMACY_QUERY } from './pharmacy.profileQueries.js';
  6  | 
  7  | test.describe('GraphQL: Get My Pharmacy', () => {
  8  |   test(
  9  |     'PHARMA-150 | Should be able to get myPharmacy as Pharmacist',
  10 |     {
  11 |       tag: ['@api', '@pharmacist', '@positive', '@pharma-150', '@smoke'],
  12 |     },
  13 |     async ({ api }) => {
  14 |       const { accessToken, raw: loginRes } = await loginAsPharmacistAndGetTokens(api, getPharmacistCredentials('reg01'));
  15 |       expect(loginRes.ok, loginRes.error || 'Pharmacist login failed').toBe(true);
  16 | 
  17 |       const myPharmacyRes = await safeGraphQL(api, {
  18 |         query: PHARMACIST_GET_MY_PHARMACY_QUERY,
  19 |         headers: bearer(accessToken),
  20 |       });
  21 |       expect(myPharmacyRes.ok, myPharmacyRes.error || 'Failed to get pharmacist pharmacy').toBe(true);
  22 | 
  23 |       const node = myPharmacyRes.body.data.pharmacy.myPharmacy;
  24 |       expect(node).toBeTruthy();
  25 |       expect(typeof node.id).toBe('string');
  26 |       expect(typeof node.name).toBe('string');
  27 |     }
  28 |   );
  29 | 
  30 |   test(
  31 |     'PHARMA-151 | Should NOT be able to get myPharmacy as Pharmacist with No Auth',
  32 |     {
  33 |       tag: ['@api', '@pharmacist', '@negative', '@pharma-151'],
  34 |     },
  35 |     async ({ api, noAuth }) => {
  36 |       const myPharmacyResNoAuth = await safeGraphQL(api, {
  37 |         query: PHARMACIST_GET_MY_PHARMACY_QUERY,
  38 |         headers: noAuth,
  39 |       });
  40 |       expect(myPharmacyResNoAuth.ok, 'Request with no auth should not be successful').toBe(false);
  41 |       const { message, classification, code } = getGQLError(myPharmacyResNoAuth);
> 42 |       expect(message).toMatch(NOAUTH_MESSAGE_PATTERN);
     |                       ^ Error: expect(received).toMatch(expected)
  43 |       expect(NOAUTH_CLASSIFICATIONS).toContain(classification);
  44 |       expect(NOAUTH_CODES).toContain(code);
  45 |     }
  46 |   );
  47 | 
  48 |   test(
  49 |     'PHARMA-152 | Should NOT be able to get myPharmacy as Pharmacist with Invalid Auth',
  50 |     {
  51 |       tag: ['@api', '@pharmacist', '@negative', '@pharma-152'],
  52 |     },
  53 |     async ({ api, invalidAuth }) => {
  54 |       const myPharmacyResInvalidAuth = await safeGraphQL(api, {
  55 |         query: PHARMACIST_GET_MY_PHARMACY_QUERY,
  56 |         headers: invalidAuth,
  57 |       });
  58 |       expect(
  59 |         myPharmacyResInvalidAuth.ok,
  60 |         myPharmacyResInvalidAuth.error || 'Request with invalid auth should not be successful'
  61 |       ).toBe(false);
  62 | 
  63 |       // Transport-level 401 (no GraphQL errors[])
  64 |       expect(myPharmacyResInvalidAuth.ok).toBe(false);
  65 |       expect(myPharmacyResInvalidAuth.httpOk).toBe(false);
  66 |       expect(NOAUTH_HTTP_STATUSES).toContain(myPharmacyResInvalidAuth.httpStatus);
  67 |     }
  68 |   );
  69 | 
  70 |   test(
  71 |     'PHARMA-548 | Get my pharmacy should satisfy response contract shape',
  72 |     {
  73 |       tag: ['@api', '@pharmacist', '@positive', '@pharma-548'],
  74 |     },
  75 |     async ({ api }) => {
  76 |       const { accessToken, raw: loginRes } = await loginAsPharmacistAndGetTokens(api, getPharmacistCredentials('reg01'));
  77 |       expect(loginRes.ok, loginRes.error || 'Pharmacist login failed').toBe(true);
  78 | 
  79 |       const myPharmacyRes = await safeGraphQL(api, {
  80 |         query: PHARMACIST_GET_MY_PHARMACY_QUERY,
  81 |         headers: bearer(accessToken),
  82 |       });
  83 |       expect(myPharmacyRes.httpStatus).toBe(200);
  84 |       expect(myPharmacyRes.httpOk).toBe(true);
  85 |       expect(myPharmacyRes.ok, myPharmacyRes.error || 'Failed to get pharmacist pharmacy').toBe(true);
  86 | 
  87 |       const node = myPharmacyRes.body?.data?.pharmacy?.myPharmacy;
  88 |       expect(node, 'Missing data.pharmacy.myPharmacy').toBeTruthy();
  89 |       expect.soft(typeof node?.id).toBe('string');
  90 |       expect.soft(typeof node?.name).toBe('string');
  91 |     }
  92 |   );
  93 | });
  94 | 
```