# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: api/tests/pharmacy/chatsMessaging/pharmacist.regularPharmacistChats.spec.js >> GraphQL: Regular Pharmacist Messaging >> PHARMA-534 | Regular pharmacist get chat thread should satisfy response contract shape
- Location: api/tests/pharmacy/chatsMessaging/pharmacist.regularPharmacistChats.spec.js:202:7

# Error details

```
Error: 401: Unauthorized access

expect(received).toBe(expected) // Object.is equality

Expected: true
Received: false
```

# Test source

```ts
  123 |       expect(getChatMessageByThreadIdRes.ok).toBe(true);
  124 |     }
  125 |   );
  126 | 
  127 |   test(
  128 |     'PHARMA-201 | Should be able to send Chat message by Order ID as Regular Pharmacist',
  129 |     {
  130 |       tag: ['@api', '@pharmacist', '@positive', '@pharma-201'],
  131 |     },
  132 |     async ({ api }) => {
  133 |       const { accessToken, raw: loginRes } = await loginAsPharmacistAndGetTokens(api, getPharmacistCredentials('reg01'));
  134 |       expect(loginRes.ok, loginRes.error || 'Pharmacist login failed').toBe(true);
  135 | 
  136 |       const testData = resolveTestData();
  137 | 
  138 |       const sendChatMessageByOrderIdRes = await safeGraphQL(api, {
  139 |         query: PHARMACIST_SEND_CHAT_MESSAGES_BY_ORDER_ID_QUERY,
  140 |         variables: {
  141 |           orderId: testData.orderId,
  142 |           chat: {
  143 |             sender: `PHARMACIST`,
  144 |             message: `Sample message from api - ${randomAlphanumeric(6)}`,
  145 |           },
  146 |         },
  147 |         headers: bearer(accessToken),
  148 |       });
  149 |       expect(sendChatMessageByOrderIdRes.ok).toBe(true);
  150 |     }
  151 |   );
  152 | 
  153 |   test(
  154 |     'PHARMA-202 | Should be able to send Chat message by Thread ID as Regular Pharmacist',
  155 |     {
  156 |       tag: ['@api', '@pharmacist', '@positive', '@pharma-202'],
  157 |     },
  158 |     async ({ api }) => {
  159 |       const { accessToken, raw: loginRes } = await loginAsPharmacistAndGetTokens(api, getPharmacistCredentials('reg01'));
  160 |       expect(loginRes.ok, loginRes.error || 'Pharmacist login failed').toBe(true);
  161 | 
  162 |       const testData = resolveTestData();
  163 | 
  164 |       const sendChatMessageByThreadIdRes = await safeGraphQL(api, {
  165 |         query: PHARMACIST_SEND_CHAT_MESSAGES_BY_THREAD_ID_QUERY,
  166 |         variables: {
  167 |           threadId: testData.threadId,
  168 |           chat: {
  169 |             sender: `PHARMACIST`,
  170 |             message: `Sample message from api - ${randomAlphanumeric(6)}`,
  171 |           },
  172 |         },
  173 |         headers: bearer(accessToken),
  174 |       });
  175 |       expect(sendChatMessageByThreadIdRes.ok).toBe(true);
  176 |     }
  177 |   );
  178 | 
  179 |   test(
  180 |     'PHARMA-203 | Should be able to SEEN a message thread as Regular Pharmacist',
  181 |     {
  182 |       tag: ['@api', '@pharmacist', '@positive', '@pharma-203'],
  183 |     },
  184 |     async ({ api }) => {
  185 |       const { accessToken, raw: loginRes } = await loginAsPharmacistAndGetTokens(api, getPharmacistCredentials('reg01'));
  186 |       expect(loginRes.ok, loginRes.error || 'Pharmacist login failed').toBe(true);
  187 | 
  188 |       const testData = resolveTestData();
  189 | 
  190 |       const setThreadSeenRes = await safeGraphQL(api, {
  191 |         query: PHARMACIST_SET_THREAD_SEEN_QUERY,
  192 |         variables: {
  193 |           threadId: testData.threadId,
  194 |         },
  195 |         headers: bearer(accessToken),
  196 |       });
  197 | 
  198 |       expect(setThreadSeenRes.ok).toBe(true);
  199 |     }
  200 |   );
  201 | 
  202 |   test(
  203 |     'PHARMA-534 | Regular pharmacist get chat thread should satisfy response contract shape',
  204 |     {
  205 |       tag: ['@api', '@pharmacist', '@positive', '@pharma-534'],
  206 |     },
  207 |     async ({ api }) => {
  208 |       const { accessToken, raw: loginRes } = await loginAsPharmacistAndGetTokens(api, getPharmacistCredentials('reg01'));
  209 |       expect(loginRes.ok, loginRes.error || 'Pharmacist login failed').toBe(true);
  210 | 
  211 |       const testData = resolveTestData();
  212 |       const getChatThreadIdRes = await safeGraphQL(api, {
  213 |         query: PHARMACIST_GET_CHAT_THREAD_BY_ID_QUERY,
  214 |         variables: {
  215 |           orderId: testData.orderId,
  216 |           type: `PATIENT_PHARMACY`,
  217 |         },
  218 |         headers: bearer(accessToken),
  219 |       });
  220 | 
  221 |       expect(getChatThreadIdRes.httpStatus).toBe(200);
  222 |       expect(getChatThreadIdRes.httpOk).toBe(true);
> 223 |       expect(getChatThreadIdRes.ok, getChatThreadIdRes.error || 'Get chat thread failed').toBe(true);
      |                                                                                           ^ Error: 401: Unauthorized access
  224 | 
  225 |       const node = getChatThreadIdRes.body?.data?.pharmacy?.chat?.thread;
  226 |       expect(node, 'Missing data.pharmacy.chat.thread').toBeTruthy();
  227 |       expect.soft(typeof node?.id).toBe('string');
  228 |       expect.soft(typeof node?.partiesType).toBe('string');
  229 |       expect.soft(typeof node?.requesterType).toBe('string');
  230 |       expect.soft(typeof node?.supportAgentType).toBe('string');
  231 |     }
  232 |   );
  233 | 
  234 |   test(
  235 |     'PHARMA-591 | Should NOT get chat thread when required orderId variable is missing',
  236 |     {
  237 |       tag: ['@api', '@pharmacist', '@negative', '@pharma-591'],
  238 |     },
  239 |     async ({ api }) => {
  240 |       const { accessToken, raw: loginRes } = await loginAsPharmacistAndGetTokens(api, getPharmacistCredentials('reg01'));
  241 |       expect(loginRes.ok, loginRes.error || 'Pharmacist login failed').toBe(true);
  242 | 
  243 |       const getChatThreadMissingVariableRes = await safeGraphQL(api, {
  244 |         query: PHARMACIST_GET_CHAT_THREAD_BY_ID_QUERY,
  245 |         variables: { type: `PATIENT_PHARMACY` },
  246 |         headers: bearer(accessToken),
  247 |       });
  248 | 
  249 |       expect(getChatThreadMissingVariableRes.ok, 'Expected GraphQL variable validation failure').toBe(false);
  250 |       if (getChatThreadMissingVariableRes.httpOk) {
  251 |         const { message, code, classification } = getGQLError(getChatThreadMissingVariableRes);
  252 |         expect(message, 'Expected GraphQL validation message for missing orderId').toBeTruthy();
  253 |         expect.soft(code || classification, 'Expected GraphQL error code/classification').toBeTruthy();
  254 |       } else {
  255 |         expect.soft(getChatThreadMissingVariableRes.httpStatus).toBeGreaterThanOrEqual(400);
  256 |       }
  257 |     }
  258 |   );
  259 | });
  260 | 
```