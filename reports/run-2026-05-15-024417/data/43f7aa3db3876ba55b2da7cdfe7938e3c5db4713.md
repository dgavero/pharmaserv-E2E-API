# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: api/tests/patient/chatMessaging/patient.getChatMessagesByThreadId.spec.js >> GraphQL: Patient Get Chat Messages By Thread Id >> PHARMA-271 | Should NOT get chat messages by thread id with missing auth tokens
- Location: api/tests/patient/chatMessaging/patient.getChatMessagesByThreadId.spec.js:31:7

# Error details

```
Error: expect(received).toContain(expected) // indexOf

Expected value: 503
Received array: [401, 404, 403, 500]
```

# Test source

```ts
  1   | import { loginAsPatientAndGetTokens, NOAUTH_MESSAGE_PATTERN, NOAUTH_CLASSIFICATIONS, NOAUTH_CODES, NOAUTH_HTTP_STATUSES } from '../../../helpers/auth.js';
  2   | import { safeGraphQL, bearer, getGQLError } from '../../../helpers/graphqlUtils.js';
  3   | import { test, expect } from '../../../globalConfig.api.js';
  4   | import { getPatientCredentials } from '../../../helpers/roleCredentials.js';
  5   | import { GET_CHAT_MESSAGES_BY_THREAD_ID_QUERY } from './patient.chatMessagingQueries.js';
  6   | import { CHAT_THREAD_ID } from './patient.chatMessagingConstants.js';
  7   | 
  8   | test.describe('GraphQL: Patient Get Chat Messages By Thread Id', () => {
  9   |   test(
  10  |     'PHARMA-270 | Should get chat messages by thread id with valid auth tokens',
  11  |     {
  12  |       tag: ['@api', '@patient', '@positive', '@pharma-270'],
  13  |     },
  14  |     async ({ api }) => {
  15  |       const { accessToken, raw: loginRes } = await loginAsPatientAndGetTokens(api, getPatientCredentials('default'));
  16  |       expect(loginRes.ok, loginRes.error || 'Patient login failed').toBe(true);
  17  | 
  18  |       const getChatMessagesRes = await safeGraphQL(api, {
  19  |         query: GET_CHAT_MESSAGES_BY_THREAD_ID_QUERY,
  20  |         variables: { threadId: CHAT_THREAD_ID },
  21  |         headers: bearer(accessToken),
  22  |       });
  23  | 
  24  |       expect(
  25  |         getChatMessagesRes.ok,
  26  |         getChatMessagesRes.error || 'Get chat messages by thread id endpoint failed'
  27  |       ).toBe(true);
  28  |     }
  29  |   );
  30  | 
  31  |   test(
  32  |     'PHARMA-271 | Should NOT get chat messages by thread id with missing auth tokens',
  33  |     {
  34  |       tag: ['@api', '@patient', '@negative', '@pharma-271'],
  35  |     },
  36  |     async ({ api, noAuth }) => {
  37  |       const getChatMessagesNoAuthRes = await safeGraphQL(api, {
  38  |         query: GET_CHAT_MESSAGES_BY_THREAD_ID_QUERY,
  39  |         variables: { threadId: CHAT_THREAD_ID },
  40  |         headers: noAuth,
  41  |       });
  42  | 
  43  |       expect(
  44  |         getChatMessagesNoAuthRes.ok,
  45  |         getChatMessagesNoAuthRes.error || 'Expected UNAUTHORIZED when missing token'
  46  |       ).toBe(false);
  47  | 
  48  |       if (!getChatMessagesNoAuthRes.httpOk) {
> 49  |         expect(NOAUTH_HTTP_STATUSES).toContain(getChatMessagesNoAuthRes.httpStatus);
      |                                      ^ Error: expect(received).toContain(expected) // indexOf
  50  |       } else {
  51  |         const { message, code, classification } = getGQLError(getChatMessagesNoAuthRes);
  52  |         expect(message).toMatch(NOAUTH_MESSAGE_PATTERN);
  53  |         expect.soft(NOAUTH_CODES).toContain(code);
  54  |         expect.soft(NOAUTH_CLASSIFICATIONS).toContain(classification);
  55  |       }
  56  |     }
  57  |   );
  58  | 
  59  |   test(
  60  |     'PHARMA-272 | Should NOT get chat messages by thread id with invalid auth tokens',
  61  |     {
  62  |       tag: ['@api', '@patient', '@negative', '@pharma-272'],
  63  |     },
  64  |     async ({ api, invalidAuth }) => {
  65  |       const getChatMessagesInvalidAuthRes = await safeGraphQL(api, {
  66  |         query: GET_CHAT_MESSAGES_BY_THREAD_ID_QUERY,
  67  |         variables: { threadId: CHAT_THREAD_ID },
  68  |         headers: invalidAuth,
  69  |       });
  70  | 
  71  |       expect(getChatMessagesInvalidAuthRes.ok).toBe(false);
  72  |       expect(getChatMessagesInvalidAuthRes.httpOk).toBe(false);
  73  |       expect(NOAUTH_HTTP_STATUSES).toContain(getChatMessagesInvalidAuthRes.httpStatus);
  74  |     }
  75  |   );
  76  | 
  77  |   test(
  78  |     'PHARMA-487 | Chat messages by thread id should satisfy list-item contract shape',
  79  |     {
  80  |       tag: ['@api', '@patient', '@positive', '@pharma-487'],
  81  |     },
  82  |     async ({ api }) => {
  83  |       const { accessToken, raw: loginRes } = await loginAsPatientAndGetTokens(api, getPatientCredentials('default'));
  84  |       expect(loginRes.ok, loginRes.error || 'Patient login failed').toBe(true);
  85  | 
  86  |       const getChatMessagesRes = await safeGraphQL(api, {
  87  |         query: GET_CHAT_MESSAGES_BY_THREAD_ID_QUERY,
  88  |         variables: { threadId: CHAT_THREAD_ID },
  89  |         headers: bearer(accessToken),
  90  |       });
  91  | 
  92  |       expect(getChatMessagesRes.httpStatus).toBe(200);
  93  |       expect(getChatMessagesRes.httpOk).toBe(true);
  94  |       expect(
  95  |         getChatMessagesRes.ok,
  96  |         getChatMessagesRes.error || 'Get chat messages by thread id endpoint failed'
  97  |       ).toBe(true);
  98  | 
  99  |       const node = getChatMessagesRes.body?.data?.patient?.chat?.threadMessages;
  100 |       expect(Array.isArray(node), 'threadMessages should be an array').toBe(true);
  101 |       if (node.length > 0) {
  102 |         expect.soft(typeof node[0]?.sender).toBe('string');
  103 |         expect.soft(typeof node[0]?.recipient).toBe('string');
  104 |         if (node[0]?.message !== null) {
  105 |           expect.soft(typeof node[0]?.message).toBe('string');
  106 |         }
  107 |         if (node[0]?.photo !== null) {
  108 |           expect.soft(typeof node[0]?.photo).toBe('string');
  109 |         }
  110 |         expect.soft(typeof node[0]?.createdAt).toBe('string');
  111 |       }
  112 |     }
  113 |   );
  114 | });
  115 | 
```