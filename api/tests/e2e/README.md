# API E2E Workflows

This directory contains cross-role API workflow tests.

- `shared/` contains reusable workflow runner, context, steps, queries, and assertions.
- `deliverx/`, `pabili/`, and `findMyMeds/` contain scenario definitions and specs.
