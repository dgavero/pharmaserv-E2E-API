export async function runWorkflow({ context, scenario, registry }) {
  // TODO: orchestrate scenario steps using role/action registry.
  // Intentionally scaffold-only for now.
  return { context, scenario, registry };
}
